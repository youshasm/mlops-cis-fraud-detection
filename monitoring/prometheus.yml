global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: fraud-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - fraud-api:8000
        labels:
          service: fraud-detection
